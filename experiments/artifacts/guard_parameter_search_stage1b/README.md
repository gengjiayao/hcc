# GUARD development parameter search

This directory preserves a frozen development-stage search. It is not a held-out performance claim. `selection.json` applies the preregistered paired-CI gates; raw outputs remain under the repository-relative locators in `run_registry.csv`.
